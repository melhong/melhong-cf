---
label: post
hide_body: false
fields:
- type: select
  config:
    source:
      type: simple
      section: jekyll-posts
    required: true
    options:
    - post
  name: layout
  description: Layout can be type of content you are posting
  hidden: false
  default: post
- type: text
  name: title
  description: post title
  label: post title
  config:
    required: true
- type: tag_list
  name: category
  label: Category
  description: Hit enter to add multiple category.
- type: file
  name: thumbnail
  label: thumbnail
  description: thumbnail picture
  config:
    maxSize: '1'
- type: text
  label: Featured video
  name: featuredvideo
  description: Put you featured video link here
  config:
    required: true
